{
  "name": "mp4decrypt-buffer",
  "version": "1.0.0",
  "description": "Native NodeJS module to decrypt media in-memory",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mp4", "hls", "dash", "cenc", "native"],
  "author": "ubuntuegor",
  "license": "GPL-2.0",
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^4.0.0"
  }
}
